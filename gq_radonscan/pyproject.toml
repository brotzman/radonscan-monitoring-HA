[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "radon-monitoring"
version = "5.3.1"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["rootfs/usr/local/lib"]
testpaths = ["tests"]
addopts = "-ra"
